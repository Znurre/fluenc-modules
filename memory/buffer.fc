namespace Memory
{
    struct Buffer;

    import Buffer malloc(i32 size);
    import Buffer realloc(Buffer ptr, i32 new_size);
    import Buffer empty();

    import bool isEmpty(Buffer buffer);

    import buffer allocate(i64 size);
    import buffer reallocate(buffer buf, i64 size);
}
